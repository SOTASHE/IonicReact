import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mock_project_setup',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
